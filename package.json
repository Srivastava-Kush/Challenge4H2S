{
  "name": "challenge4-root",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm --workspace=backend run dev\" \"npm --workspace=frontend run dev\"",
    "build": "npm --workspace=frontend run build",
    "start": "npm --workspace=backend start"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "concurrently": "^10.0.3",
    "oxlint": "^1.71.0"
  },
  "engines": {
    "node": ">=22"
  }
}
